from typing import List


def toporogical_sort(edges: List[List[int]]) -> List[int]:
    """Topological sort

    Args:
        edges (List[List[int]]): An integer array represents order where edges[i] = [a_i, b_i, ...] indicates that you must place i first in the result sorted list before you place a_i, b_i, ... .
    Returns:
        List[int]: A sorted integers in given order.
    """
    pass
